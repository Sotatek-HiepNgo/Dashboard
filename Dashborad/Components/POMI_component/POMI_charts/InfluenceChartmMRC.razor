@inject IJSRuntime JS
@namespace Dashboard.Components.POMI_charts

<canvas id="influenceChartmMRC"></canvas>

@code {
    private async Task RenderChart()
    {
        await JS.InvokeVoidAsync("drawInfluenceChartmMRC");
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await RenderChart();
        }
    }
}
