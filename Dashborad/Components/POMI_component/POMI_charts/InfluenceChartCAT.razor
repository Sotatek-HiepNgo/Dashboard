@inject IJSRuntime JS
@namespace Dashboard.Components.POMI_charts

<canvas id="influenceChartCAT"></canvas>

@code {
    private async Task RenderChart()
    {
        await JS.InvokeVoidAsync("drawInfluenceChartCAT");
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await RenderChart();
        }
    }
}
